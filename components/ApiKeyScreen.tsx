import React from 'react';

// This component is intentionally left blank to remove the API key screen.
// The application now assumes the API key is provided via environment variables.
export const ApiKeyScreen: React.FC = () => {
  return null;
};
